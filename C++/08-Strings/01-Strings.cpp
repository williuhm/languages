/*
    Strings are supposed to always include the <string>
    library at the top, so I'll have to make sure I do that.
*/

#include <iostream>
#include <string>
using namespace std;

int main() {
    // Create a string variable
    string greeting = "Hello and welcome!";

    // Print the string
    cout << greeting << "\n";
    return 0;
}