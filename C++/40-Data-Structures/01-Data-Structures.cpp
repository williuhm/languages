/*
    Data Structures was known for being one of the "weed-out"
    courses at college. Very difficult for a lot of people,
    and some people still complained about it nearing
    graduation.

    I was the TA for that course, and its follow-up, 
    Algorithm Analysis. Basically, this shouldn't be too
    hard.
*/

// Include the vector library
#include <vector>

// Include the list library
#include <list>

// Include the stack library
#include <stack>

// Include the queue library
#include <queue>

// Include the deque library
#include <deque>

// Include the set library
#include <set>

// Include the map library
#include <map>
