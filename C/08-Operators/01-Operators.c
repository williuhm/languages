/*
    Operators are used all the time in programming. Things like adding,
    subtracting, multiplication, division, modulus, and more.
*/

#include <stdio.h>

int main() {
    int myNum = 100 + 50;

    int sum1 = 100 + 50;        // 150 (100 + 50)
    int sum2 = sum1 + 250;      // 400 (150 + 250)
    int sum3 = sum2 + sum2;     // 800 (400 + 400)
    return 0;
}