/*
    Challenge: Check the Minimum Age
        Test your understanding of C booleans by completing a small coding challenge.

    Instructions
        Inside main(), complete the following steps:
        1. Declare two int variables named age and minAge, and assign them values
        2. Use printf to print the result of this comparison: age >= minAge
*/

#include <stdio.h>

int main() {
    // Declare and assign age
    int age = 23;
    // Declare and assign minAge
    int minAge = 21;

    // Print the result of: age >= minAge
    printf("%d\n", age >= minAge);

    return 0;
}