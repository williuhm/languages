/*
    Constants are unchanged, so this will be an unchanging value.

    Convention to make them all capitalized.
*/

#include <stdio.h>

int main() {
    const int minutesPerHour = 60;
    const int monthsInYear = 12;

    const int BIRTHYEAR = 1980;
    return 0;
}