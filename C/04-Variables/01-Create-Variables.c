/*
    Creating variables requires a declaration and assignment. So
    I assume there will be some "int x = 10" sort of deals around here.

    Printing myNum will throw an error, different from C++, Java, and
    Python. I think this involves the formatting of the print using
    commas.
*/

#include <stdio.h>

int main() {
    // Declare a variable
    int myNum;

    // Assign a value later
    myNum = 15;
    return 0;
}